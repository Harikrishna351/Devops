shell commands:

LS
cp
mv
mkdir
touch
vim
grep
list ing -ls
top sar
ps -ef
ps -ef | awk -F" "'{print $id}

print errors from a remote log
curl pipe and grep
ex: curl url | grep error

script to write divisible by 3,5 and not by 15?

for i in {1 ..100}; do
if ['expr $i%3 == 0] || ['expr $i % 5' == 0]) && ['expr $i % 15' != 0];

then 
    echo $i
fi;
done

write a script to print number of 's' in 'mississippi'?

x = mississippi

grep -o "s" <<<"$x" | wc -l

set -x

open a file in a read-only file
vim -r test.txt

hard and soft links?
hard links are used even if the file got deleted using hard link can be retrieved

network troubleshoot 
traceroute
tracepath

how to sort list of names in a file?

Sort
howto manage logs of a system that generates huge log files everyday?

logrotate(gzip,zip)




